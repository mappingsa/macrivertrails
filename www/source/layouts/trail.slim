/ Layout for Trail (list) pages
= partial :head
body
  = blank_line
  @page.trail-page class="#{current_page.data.section}" data-dom-cache="true"
    = partial :trail_header
    @content data-iscroll="true"
      = blank_line
      .flex-container style="width: 90%; min-height: 250px; text-align: center; margin: auto; padding-top: 10px;"
        .flexslider.pageImageSlider
          ul.slides
            // TODO: Flexslider has a bug where it shows the last slide first
            // If/when it is fixed, remove the "rotate" in the line below
            - current_page.data.list.rotate.each do |place|
              = partial :trail_slide, :locals => {:place => place}
      = blank_line
      ul.trail-list.big-arrow@listview data-inset=true
        - current_page.data.list.each do |place|
          = partial :place, :locals => {:place=> place}
          = next_line
      br
    = partial :tabbar
