/ head for most parts of the site
=blank_line
doctype 5
html lang="en-AU"
  = partial :middleman_info, :locals => { :is_head => true }
  head
    = blank_line
    title = current_page.data.title
    / PhoneGap build: only generate resources on home page
    / Non-Phonegap build: generate resources on every page
    - if (!(build_target =~ /phonegap/)) || (current_resource.path == 'index.html')
      = partial :meta
      = stylesheet_link_tag data.rivertrails[build_target].stylesheets
      = blank_line
      = javascript_include_tag data.rivertrails[build_target].scripts
      /script src="js/require-config.js"
      /script src="js/require-2.1.4-jquery-1.8.2.js"
      = blank_line
= blank_line
