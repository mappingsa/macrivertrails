# See http://help.github.com/ignore-files/ for more about ignoring files.
#
# If you find yourself ignoring temporary files generated by your text editor
# or operating system, you probably want to add a global ignore instead:
# git config --global core.excludesfile ~/.gitignore_global

# apple nuisance files
.DS_Store
.AppleDouble

# Windows nuisance files
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/

# We generally want to ignore these
.bak
.temp

# Eclipse settings file - these are individual user preferences, so should not be checked-in
.settings/
.metadata

# Sublime Text 2 workspace and settings files - similar to Eclipse .settings
# i.e. these are user preferences
# .sublime-project files *should* be checked-in, however
*.sublime-workspace

# bundler config
www/.bundle

# Sass cache
*.sass-cache
www/build/

# RVM per-project/diretory settings
*.rvmrc

