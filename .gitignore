# See http://help.github.com/ignore-files/ for more about ignoring files.
#
# If you find yourself ignoring temporary files generated by your text editor
# or operating system, you probably want to add a global ignore instead:
# git config --global core.excludesfile ~/.gitignore_global

# apple nuisance files
.DS_Store
.AppleDouble
*.swp
*.lock
!Gemfile.lock
profile

# Windows nuisance files
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/

# We generally want to ignore these
.bak
.temp

# Eclipse settings file - these are individual user preferences, so should not be checked-in
.settings/
.metadata

# Sublime Text 2 workspace and settings files - similar to Eclipse .settings
# i.e. these are user preferences
# .sublime-project files *should* be checked-in, however
*.sublime-workspace

# bundler config
www/.bundle

# Sass cache
*.sass-cache
www/build/

# RVM per-project/diretory settings
*.rvmrc

# XCode private settings
*.pbxuser
*.mode1v3
*.mode2v2
*.perspectivev3
xcuserdata

# XCode temporary files that should never be committed
*~.nib

# Phonegap www directory content pointed-to by symlink
phonegap/ios/www/*
# For now, ignore the symlink as well, as it isn't commiting correctly
phonegap/ios/www
phonegap/android/assets/www/*
phonegap/android/assets/www

# Ignore Phonegap project.pbxproj. You need to create a symlink to the appropriate developer-specific
# file - i.e. project.jp.pbxproj or project.jt.pbxproj
phonegap/ios/macriverTRAILS.xcodeproj/project.pbxproj



.ruby-gemset
/www/.ruby-version
phonegap/macriverTRAILS/www
